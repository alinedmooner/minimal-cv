{
  "name": "minimal-cv",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "stylelint": "^15.10.3",
    "stylelint-config-manzdev": "^0.0.10",
    "vite": "^4.4.5"
  }
}
